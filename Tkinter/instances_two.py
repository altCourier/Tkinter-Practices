from tkinter import * # Call all into the global namepace.
# No need to use the prefix.

root = Tk()
root.title("Fridge")

magnet = Label(root, text = "Magnet")
magnet.pack()

root.mainloop()